# Local development — set this to your machine's LAN IP so phones can connect.
# Find your IP:  Windows → ipconfig   |   Mac/Linux → ifconfig
# Example: VITE_BACKEND_URL=http://192.168.1.5:4000
VITE_BACKEND_URL=http://192.168.29.76:4000
